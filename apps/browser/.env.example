# Routely browser IDE — build-time API base URL.
# Leave empty when served from the same origin as the API (e.g. FastAPI /web/browser/).
# Production (Vercel): https://api.routely.aitotech.in
VITE_ROUTELY_API_URL=
